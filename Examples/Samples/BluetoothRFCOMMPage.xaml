<Page
    x:Class="Examples.Samples.BluetoothRFCOMMPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Examples.Samples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Button Content="Request Access" Click="Button_Click"/>
                <ToggleSwitch Header="Enabled" Toggled="ToggleSwitch_Toggled"/>
                <Button Content="Refresh Devices" Click="Button_Click_1"/>
                <ListView x:Name="DevicesList" IsItemClickEnabled="True" ItemClick="DevicesList_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock FontSize="{StaticResource TextStyleExtraLargeFontSize}" Text="{Binding Name}"/>
                                <TextBlock FontSize="{StaticResource TextStyleLargeFontSize}" Text="{Binding Address}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
